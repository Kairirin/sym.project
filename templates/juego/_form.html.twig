{{ form_start(form) }}
	<div class="container"> <div class="my-custom-class-for-errors">
		{{ form_errors(form) }}
	</div>
	<div class="row">
		<div class="col-md-8">
			{{ form_row(form.nombre) }}
		</div>
		<div class="col-md-4">
			{{ form_row(form.plataforma,{'label':'Plataforma:','label_attr': {'class':'etiqueta'}}) }}
		</div>
	</div>
	<div class="row">
		{{ form_row(form.imagen,{'label':'Imagen:','label_attr': {'class':'etiqueta'}}) }} {#TODO: Añadir a class las clases de bootrstrap que tiene el formulario #}
	</div>
	<div class="row">
		<button class="btn">{{ button_label|default('Guardar') }}</button>
	</div>
</div>
{{ form_end(form) }}
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_errors(form) }}
{{ form_end(form) }}
