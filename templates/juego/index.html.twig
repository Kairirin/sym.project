{% extends 'base.html.twig' %}

{% block title %}Videojuegos{% endblock %}

{% block principal %}
    <!-- Header Start -->
    <div class="container-fluid bg-breadcrumb">
        <ul class="breadcrumb-animation">
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
            <li></li>
        </ul>
        <div class="container text-center py-5" style="max-width: 900px;">
            <h3 class="display-3 mb-4 wow fadeInDown" data-wow-delay="0.1s">Videojuegos</h1>
                <ol class="breadcrumb justify-content-center mb-0 wow fadeInDown" data-wow-delay="0.3s">
                    <li class="breadcrumb-item"><a href="/playstation">Playstation</a></li>
                    <li class="breadcrumb-item"><a href="/xbox">Xbox</a></li>
                    <li class="breadcrumb-item"><a href="/nintendo">Nintendo</a></li>
                    <li class="breadcrumb-item"><a href="/retro">Retro</a></li>
                </ol>
        </div>
    </div>
    <!-- Header End -->

    <!-- Pricing Start -->
    <div class="container-fluid price py-5">
        <div class="container py-5">
            <div class="text-center mx-auto mb-5 wow fadeInUp" data-wow-delay="0.1s" style="max-width: 900px;">
                <h4 class="text-primary">{{ sobretitulo }}</h4>
                <h1 class="display-5 mb-4">{{ h1Pagina }}</h1>
            </div>
            <div class="row g-5 justify-content-center">
            {% if (juegos != null) %}
                {% for videojuego in juegos %}
                <div class=" col-md-6 col-lg-6 col-xl-4 wow fadeInUp" data-wow-delay="0.1s">
                            <div class="tarjeta price-item bg-light rounded text-center">
                                <div class="text-center text-dark border-bottom d-flex flex-column justify-content-center p-4" style="width: 100%; height: 100px;">
                                    <p class="fs-2 fw-bold text-uppercase mb-0">{{ videojuego.nombre }}</p>
                                </div>
                                <img src="{{ asset(videojuego.getUrlPortada()) }}" alt="{{ videojuego.nombre }}">
                                <!-- <div class="tarjeta d-flex justify-content-center">
                            </div> -->
                                <div class="text-start p-5">
                                    <p><i class="fas fa-check text-success me-1"></i> Plataforma: {{ videojuego.plataforma }}</p>
                                    <p><i class="fas fa-check text-success me-1"></i> Reviews: </p>
                                    <a href="{{ path('app_juego_show', {'id': videojuego.id}) }}"><button class="btn btn-light rounded-pill py-2 px-5" type="button">Ver reviews</button></a>
                                </div>
                            </div>
                        </div>
                {% endfor %}
            {% else %}
                <p class="mb-0 text-center">Anímate y sé el primero en aportar tu videojuego favorito
                </p>
            {% endif %}
            </div>
        </div>
    </div>
    <!-- Pricing End -->

<!--TODO: Falta que si es admin, podrá acceder al formulario de añadir nuevo-->
{% endblock %}

{# {% block principal %}
    <h1>Videojuegos</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Imagen</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for juego in juegos %}
            <tr>
                <td>{{ juego.id }}</td>
                <td>{{ juego.nombre }}</td>
                <td><img src="{{ asset(juego.getUrlPortada()) }}" alt="{{ juego.nombre }}" width="100px"></td>
                <td>
                    <a href="{{ path('app_juego_show', {'id': juego.id}) }}">show</a>
                    <a href="{{ path('app_juego_edit', {'id': juego.id}) }}">edit</a>
                    <a href="{{ path('app_imagen_delete_json', {'id': imagen.id}) }}" class="delete">borrar</a>
                </td>
                
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_juego_new') }}">Create new</a>
{% endblock %} #}
