{% extends 'base.html.twig' %}

{% block title %}{{ user.username }}{% endblock %}

{% block principal %}
    <div class="container-fluid bg-breadcrumb">
    <ul class="breadcrumb-animation">
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
    </ul>
    <div class="container text-center py-5" style="max-width: 500px;">
        <div class="testimonial-inner-img border border-primary border-3 me-4" style="width: 100px; height: 100px; border-radius: 50%;">
            <img src="{{ asset(user.getUrlAvatar()) }}" class="img-fluid rounded-circle" alt="">
        </div>
        <h3 class="display-3 mb-4 wow fadeInDown" data-wow-delay="0.1s">{{ user.username }}</h1>
    </div>
</div>
<!-- Header End -->

<div class="container-fluid contact py-5">
    <div class="container py-5">
        <div class="row g-5 align-items-center">
            {% if app.user and (user.username == app.user.username or is_granted('ROLE_ADMIN')) %}
            <div class="col-lg-12 wow fadeInLeft" data-wow-delay="0.1s">
                <a href="{{ path('user_edit', {'id': user.id}) }}">
				<button class="btn btn-light rounded-pill py-2 px-5" type="button">Editar perfil</button>
			</a>
            </div>
            {% endif %}
            <div class="col-lg-12 wow fadeInRight" data-wow-delay="0.3s">
                <h2 class="display-5 mb-2">Reseñas</h2>
                {{ render(controller('App\\Controller\\ReviewController::show', { 'user': user })) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
